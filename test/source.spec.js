const chai = require('chai');
const Source = require('../lib/source');

chai.should();

describe('Source', () => {
  describe('#()', () => {
    it('', () => {});
  });

  describe('#createOutput()', () => {
    it('', () => {});
  });
});
