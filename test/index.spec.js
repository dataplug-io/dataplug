const chai = require('chai');
const Source = require('../lib');

chai.should();

describe('dataplug', () => {
  describe('#()', () => {
    it('', () => {});
  });

  describe('#createOutput()', () => {
    it('', () => {});
  });
});
